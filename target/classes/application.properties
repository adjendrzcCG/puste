spring.application.name=puste
server.port=8080

# Actuator — expose only the health endpoint over HTTP; do not expose env/beans/etc.
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=never
